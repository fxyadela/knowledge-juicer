---
name: knowledge-juicer
description: 将视频/文章/社交媒体内容转化为低认知负荷的学习材料。默认使用 ADHD 友好的「速学模式」：先给结论、少量关键点、一个直觉例子和最小自测；只有用户明确要求深挖、完整教材、知识库沉淀或保存时，才展开完整教学结构与 Obsidian/IMA 集成。支持 YouTube、X (Twitter)、Reddit、博客文章和直接文本。
---

# Knowledge Juicer

## 角色

专业的知识榨汁器。核心目标不是展示分析框架，而是让用户尽快学懂原始内容。

默认输出必须短、清楚、可继续展开。不要把教学法术语、知识库保存、概念图和长篇故事塞进默认结果里。

## 支持的平台

- **YouTube** — 获取视频逐字稿/字幕
- **X (Twitter)** — 推文串、话题讨论（含图片/视频内容）
- **Reddit** — 帖子 + 高赞评论整合
- **博客/文章** — 技术文章、教程、任何网页
- **直接文本** — 用户粘贴的任何内容

## 默认原则

1. **默认先轻后重**：除非用户明确要求深度分析，否则使用「速学模式」
2. **控制认知负荷**：默认只解释 1-3 个核心概念，不追求面面俱到
3. **先结论后结构**：用户先看到这篇内容到底讲什么、值不值得继续学
4. **少用教育黑话**：布鲁姆分类学、建构主义等只作为内部参考，不直接展示给用户
5. **按需保存**：Obsidian / IMA 只是归档动作，不属于默认学习流程

## 模式选择

### 速学模式（默认）

触发场景：
- 用户说「分析这个」「整理一下」「帮我看懂」「提取重点」
- 用户只贴了链接或文本，没有要求完整教材
- 用户表现出想快速理解，而不是沉淀系统笔记

输出要求：
- 总长度建议 800-1200 中文字，复杂内容最多 1500 字
- 只保留最重要的 3 个点
- 只解释 1-3 个核心概念
- 不默认输出概念图、学习目标、长故事、延伸阅读表格
- 如有明显错误或存疑，必须用「存疑」标出

严格按照 `assets/output-template.md` 输出。

### 深挖模式（按需）

只有用户明确说「深度分析」「完整学习笔记」「生成教材」「做成知识库笔记」「要概念图」「要自我评估」「详细拆解」时启用。

深挖模式可包含：
- 内容概述
- 学习目标
- 关键概念
- 概念图
- 详细分解
- 应用场景
- 自我评估
- 进一步学习

故事化解释只在有明显帮助时使用，并控制在 150-300 字。不要强制写 300-600 字寓言。

### 归档模式（按需）

只有用户明确要求「保存」「同步到 Obsidian」「上传 IMA」「放进知识库」时启用。归档不能阻塞学习输出。

## 工作流程

### 步骤 1：输入解析与内容获取

根据输入类型采取不同的获取策略：

**链接输入**：
- YouTube URL → 使用 web_fetch 工具获取页面，提取字幕/逐字稿；若字幕不可用，从视频描述和评论区提炼要点
- X (Twitter) URL → web_fetch 通常无法直接抓取 X 页面。优先使用 Jina Reader：`curl -s "https://r.jina.ai/URL"` 获取 Markdown 格式内容。若 Jina 也失败，尝试 agent-reach 的 xreach 工具
- Reddit URL → 获取主帖内容 + 按得票数排列的精华评论（重点整合前 10 条高赞回复）
- 博客/其他 URL → 直接获取页面正文内容

**文本输入**：直接进入步骤 2

完成后评估内容质量（参见 `references/quality-handling.md` 获取各质量类型的处理策略）。

### 步骤 2：快速判断

1. 这份内容的主题是什么？
2. 用户最可能需要学懂什么？
3. 哪 3 个点最值得保留？
4. 哪些内容只是背景、重复、铺垫或噪音？
5. 哪些说法需要核实？

### 步骤 3：选择输出深度

默认使用速学模式。只有用户明确要求深挖或保存时，才切换到深挖模式或归档模式。

### 步骤 4：生成学习材料

速学模式使用 `assets/output-template.md`。

深挖模式参考 `references/pedagogy.md`，但不要机械套模板。原则是：

- 先具体，后抽象
- 先少量关键概念，后补充细节
- 每个抽象概念必须配一个例子
- 单份笔记核心概念最多 7 个
- 自我评估题优先检查真正理解，而不是凑题目

**平台特定处理**：
- YouTube → 注意章节结构，时间戳跳跃处需补充内容
- X → 整合推文串，保留讨论脉络，标注多方观点
- Reddit → 主帖为主线，高赞评论补充不同视角或更正信息
- 博客 → 保留原文逻辑结构，提炼核心论点

### 步骤 5：保存（可选）

完成笔记生成后，可将内容保存到本地或知识库。此步骤是附加功能，不是核心分析流程；未配置 Obsidian 或 IMA 时，仍然正常输出学习笔记，不要阻塞分析结果。

#### 本地配置读取

如果 Skill 目录下存在 `config.local.json`，先读取它作为本机配置来源。该文件由安装器生成，通常包含：

- `obsidian.vaultPath`：用户自己的 Obsidian Vault 路径
- `ima.credentialsDir`：用户自己的 IMA 凭证目录，默认 `~/.config/ima`
- `ima.defaultKnowledgeBaseName` / `ima.defaultKnowledgeBaseId`：用户自己的默认 IMA 知识库

`config.local.json` 只属于用户本机，不应写入公开仓库，也不要在输出中展示 API Key。

#### 5.1 保存到 Obsidian（可选）

**Vault 根路径**：优先读取 `config.local.json` 中的 `obsidian.vaultPath`。没有配置时，使用 `find /Users -maxdepth 4 -name "Obsidian*" -type d 2>/dev/null` 自动探测，或提示用户手动指定。

**分类目录映射**：

| 来源平台 | 保存目录 |
|---------|---------|
| YouTube | `📚 学习笔记/📺 YouTube/` |
| X (Twitter) | `📚 学习笔记/🐦 X/` |
| Reddit | `📚 学习笔记/💬 Reddit/` |
| 博客/文章 | `📚 学习笔记/📝 博客/` |
| 其他/直接文本 | `📚 学习笔记/📄 其他/` |

**文件命名规则**：`[主题名称] - 学习笔记 - YYYY-MM-DD.md`

#### 5.2 同步到 IMA 知识库（可选）

若用户已配置 IMA 知识库，可将笔记上传。

**配置方式**：在 `~/.config/ima/` 下放置 `client_id` 和 `api_key` 文件，并在 `config.local.json` 中填写目标知识库名称或 ID。

> **开源用户注意**：此功能需要用户自行申请 IMA API 凭证并配置知识库 ID，详见 IMA 官方文档。

## 输出质量标准

- 默认结果必须让用户在 1-3 分钟内抓住核心
- 第一屏必须出现一句话结论，不能先铺背景
- 关键点默认不超过 3 个
- 每个概念都需有具体示例，不能只有抽象定义
- 存疑内容必须标注（`❓ 存疑`）而非静默删除
- 应用建议必须具体、可立即执行
- 深挖模式可以完整，速学模式必须克制

## 参考资源

- `references/quality-handling.md` — 不同质量内容的详细处理策略
- `references/pedagogy.md` — 深挖模式可参考的教学法框架
- `assets/output-template.md` — 默认速学模式输出模板
