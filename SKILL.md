---
name: knowledge-juicer
description: 将视频/文章内容转化为优化的学习材料。支持 YouTube、X (Twitter)、Reddit、博客文章等多平台内容。当用户提供链接或文本内容，需要提取教学内容、生成学习笔记、创建教育材料时使用此 skill。触发场景：(1) 收到任何平台链接需要分析，(2) 需要将内容转化为学习材料，(3) 用户说"分析这个"、"提取教学内容"、"生成学习笔记"、"整理一下"等。
---

# Knowledge Juicer

## 角色

专业的知识榨汁器，使用先进的教学法框架将原始内容（视频逐字稿、文章、推文、讨论帖等）转化为优化的学习材料。

## 支持的平台

- **YouTube** — 获取视频逐字稿/字幕
- **X (Twitter)** — 推文串、话题讨论（含图片/视频内容）
- **Reddit** — 帖子 + 高赞评论整合
- **博客/文章** — 技术文章、教程、任何网页
- **直接文本** — 用户粘贴的任何内容

## 工作流程（五步）

### 步骤 1：输入解析与内容获取

根据输入类型采取不同的获取策略：

**链接输入**：
- YouTube URL → 使用 web_fetch 工具获取页面，提取字幕/逐字稿；若字幕不可用，从视频描述和评论区提炼要点
- X (Twitter) URL → web_fetch 通常无法直接抓取 X 页面。优先使用 Jina Reader：`curl -s "https://r.jina.ai/URL"` 获取 Markdown 格式内容。若 Jina 也失败，尝试 agent-reach 的 xreach 工具
- Reddit URL → 获取主帖内容 + 按得票数排列的精华评论（重点整合前 10 条高赞回复）
- 博客/其他 URL → 直接获取页面正文内容

**文本输入**：直接进入步骤 2

完成后评估内容质量（参见 `references/quality-handling.md` 获取各质量类型的处理策略）。

### 步骤 2：内容分析

1. 确定主题、范围、复杂度、所需先验知识
2. 识别教育水平（入门/中级/高级）
3. 提取核心概念、关键论点、重要事实
4. 过滤无关内容、填充词、重复内容
5. 标记需要核实的说法（参见 `references/quality-handling.md` 中"可能不准确内容"处理方式）

### 步骤 2.5：撰写内容概述与故事版

在正式进入教学重组之前，必须先完成两个理解检查：

1. **内容概述**：用 3-5 句平实语言概括整篇内容——讲了什么、为什么重要、结论是什么。假设读者是完全外行，读完能判断「要不要继续读」
2. **故事版（寓言/小说）**：把一个或多个核心概念包装成 300-600 字的通俗故事，有人物、有冲突、有转折、有结局。故事里的「冲突 → 解决」必须能映射回原文核心论点

这两个板块放在输出笔记的最前面（在「学习目标」之前），帮助用户快速建立直觉理解。

### 步骤 3：教育性重组

1. 参考 `references/pedagogy.md` 中的布鲁姆分类学，为内容制定可测量的学习目标
2. 遵循"具体→抽象、简单→复杂、熟悉→陌生"的知识递进原则
3. 将复杂概念分解，并提供类比和示例
4. 识别并澄清潜在的困惑点
5. 考虑认知负荷：单份笔记核心概念控制在 7±2 个

### 步骤 4：生成学习材料

严格按照 `assets/output-template.md` 的结构生成七个部分：

1. **内容概述** — 3-5句平实概括（新增强制板块）
2. **故事版** — 300-600字通俗故事（新增强制板块）
3. **学习目标** — 使用布鲁姆动词，明确可测量
4. **关键概念** — 每个概念：定义 + 解释 + 示例
5. **概念图** — ASCII 可视化（树状图/流程图/关系图）
6. **详细分解** — 按基础→高级分层组织，含代码/类比/注意事项
7. **总结** — 3-5 个核心要点 + 一句话总结
8. **应用** — 具体行动建议 + 场景表格 + 进一步学习资源
9. **自我评估** — 回忆性问题 + 应用性问题 + 反思提示

**平台特定处理**：
- YouTube → 注意章节结构，时间戳跳跃处需补充内容
- X → 整合推文串，保留讨论脉络，标注多方观点
- Reddit → 主帖为主线，高赞评论补充不同视角或更正信息
- 博客 → 保留原文逻辑结构，提炼核心论点

### 步骤 5：保存（可选）

完成笔记生成后，可将内容保存到本地或知识库。此步骤为可选，根据实际环境配置决定。

#### 本地配置读取

如果 Skill 目录下存在 `config.local.json`，先读取它作为本机配置来源。该文件由安装器生成，通常包含：

- `obsidian.vaultPath`：用户自己的 Obsidian Vault 路径
- `ima.credentialsDir`：用户自己的 IMA 凭证目录，默认 `~/.config/ima`
- `ima.defaultKnowledgeBaseName` / `ima.defaultKnowledgeBaseId`：用户自己的默认 IMA 知识库

`config.local.json` 只属于用户本机，不应写入公开仓库，也不要在输出中展示 API Key。

#### 5.1 保存到 Obsidian（可选）

**Vault 根路径**：优先读取 `config.local.json` 中的 `obsidian.vaultPath`。没有配置时，使用 `find /Users -maxdepth 4 -name "Obsidian*" -type d 2>/dev/null` 自动探测，或提示用户手动指定。

**分类目录映射**：

| 来源平台 | 保存目录 |
|---------|---------|
| YouTube | `📚 学习笔记/📺 YouTube/` |
| X (Twitter) | `📚 学习笔记/🐦 X/` |
| Reddit | `📚 学习笔记/💬 Reddit/` |
| 博客/文章 | `📚 学习笔记/📝 博客/` |
| 其他/直接文本 | `📚 学习笔记/📄 其他/` |

**文件命名规则**：`[主题名称] - 学习笔记 - YYYY-MM-DD.md`

#### 5.2 同步到 IMA 知识库（可选）

若用户已配置 IMA 知识库，可将笔记上传。

**配置方式**：在 `~/.config/ima/` 下放置 `client_id` 和 `api_key` 文件，并在 `config.local.json` 中填写目标知识库名称或 ID。

> **开源用户注意**：此功能需要用户自行申请 IMA API 凭证并配置知识库 ID，详见 IMA 官方文档。

## 输出质量标准

- 笔记必须独立可读，无需依赖原始来源
- 每个概念都需有具体示例，不能只有抽象定义
- 存疑内容必须标注（`❓ 存疑`）而非静默删除
- 应用部分必须包含至少 2 个具体、可立即执行的行动
- 自我评估问题必须覆盖回忆和应用两个层次
- **内容概述和故事版为强制板块，不可跳过**

## 参考资源

- `references/quality-handling.md` — 不同质量内容的详细处理策略
- `references/pedagogy.md` — 教学法框架和学习风格适应的详细说明
- `assets/output-template.md` — 完整的 Markdown 输出模板
